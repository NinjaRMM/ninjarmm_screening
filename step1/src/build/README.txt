Setting up googletest.

After checking out the submodule as well, if its the first time you run it
you should call "cmake .." inside the "step1/src/build" folder.

For subsequent uses, just call "make" and "./step1Tests" on this folder.